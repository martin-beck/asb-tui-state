# AR-1228 plan

Record and qualify the formal UI ownership/CI gate merged as asb-tui PR #71, including the
fail-closed malformed-model fixes and exact-main assurance.

Acceptance: immutable merge SHA and both required exact-main workflows are terminal-success; retain
AR-1201 as the owner of broader source/model parity.
