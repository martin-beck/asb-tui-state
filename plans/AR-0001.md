# AR-0001: Adopt Agent Workflow Quality v0.32.0

## Outcome

Run the exact authenticated AWQ v0.32.0 release as an additive shadow quality system for asb-tui
without weakening, replacing or relabeling any native project gate.

## Scope

Repository: `martin-beck/asb-tui`. Own only AWQ policy/lock/configuration, the isolated shadow
workflow, bounded installer/invocation glue, provenance updates and focused tests/documentation.
Do not modify product runtime behavior or existing native gate semantics.

## Work sequence

1. Authenticate the annotated v0.32.0 tag, release commit/tree, wheel, sdist, manifest, signature,
   SBOM and provenance before changing a lock.
2. Run AWQ onboarding and migration dry-runs against an exact clean asb-tui main snapshot.
3. Add the smallest reviewed core policy in shadow mode and preserve all current Rust, shell,
   workflow, privacy, provenance and trusted-main gates.
4. Record pass, failure, skip, unsupported and tool-error evidence distinctly; the shadow job must
   not convert a native failure into success.
5. Update signed dependency provenance and negative tests for version/digest/tamper failures.
6. Run exact-head local and hosted checks and obtain independent review before publication.

## Acceptance

AWQ v0.32.0 runs reproducibly and additively, all native gates remain required, failure evidence is
truthful, and no enforcement or native-gate removal is claimed without a later equivalence AR.
