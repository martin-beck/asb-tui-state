[extend]
useDefault = true

[[allowlists]]
description = "Coordinator worktree keys are public allocation metadata, not credentials"
regexTarget = "match"
regexes = ['''"worktree_key":\s+"[a-z0-9][a-z0-9-]{2,63}"''']
paths = ['''^tasks/AR-[0-9]{4}\.md$''']
